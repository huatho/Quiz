spring.application.name=Project
# Database connection
spring.datasource.url=jdbc:postgresql://localhost:5432/project_03302026
spring.datasource.username=postgres
spring.datasource.password=huatho131
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

#JWT
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_ACCESS_TOKEN_EXPIRATION:900000}

#Refresh Token
refresh-token-expiration-days=${REFRESH_TOKEN_EXPIRATION_DAYS:30}

server.port=8383